#include "../includes/philo.h"

void free_shared_info(t_arg *arg, int num_philosophers)
{
    int i;

    i = 0;
    // while (i < num_philosophers)
    // {
    //     free(arg->shared.fork[i]);
    //     i++;
    // }
}